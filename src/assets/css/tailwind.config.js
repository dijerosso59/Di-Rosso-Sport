module.exports = {
    mode: 'jit',
    purge: ['../../**/*.vue'],
    darkMode: 'class', // or 'media' or 'class'
    theme: {
      fontFamily: {
        'fredoka': ['Fredoka', 'sans-serif']
      },
      extend: {},
    },
    variants: {
      extend: {},
    },
    plugins: [],
  }